---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Build | Makeaway</title>
    <meta
      name="description"
      content="Build notes from Makeaway — side projects, websites, tools, experiments, and learning by making things."
    />
  </head>

  <body>
    <main>
      <a class="back-link" href="/">← Back to home</a>

      <p class="eyebrow">Makeaway / Build</p>

      <h1>Build</h1>

      <p class="intro">
        Side projects, websites, experiments, useful tools, and notes from
        learning by actually making things.
      </p>



      <section class="link-section">
        <h2>Build Ideas</h2>

<div class="article-list">
  <a class="article-button" href="/build/building-a-chicken-coop">
    Building a Chicken Coop
  </a>
<br></br>
  <a class="article-button" href="/build/building-a-chuck-box">
    Building a Chuck Box
  </a>
<br></br>
  <a class="article-button" href="/build/survivalist-shelters">
    Survivalist Shelters
  </a>
<br></br>
  <a class="article-button" href="/build/newsletter-signup">
    Build a Newsletter Signup Later
  </a>
<br></br>
  <a class="article-button" href="/build/digital-product-guide">
    Create a Small Digital Product or Guide
  </a>
</div>
 
    </main>
  </body>
</html>

<style>
  :root {
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", sans-serif;
    color: #f5f1e8;
    background: #172117;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background:
      radial-gradient(circle at top left, rgba(124, 185, 9, 0.25), transparent 35%),
      linear-gradient(135deg, #111811, #0c2002);
  }

  main {
    width: min(980px, calc(100% - 40px));
    margin: 0 auto;
    padding: 80px 0;
  }

  .back-link {
    display: inline-block;
    margin-bottom: 48px;
    color: #c9d8a6;
    font-weight: 700;
    text-decoration: none;
  }

  .back-link:hover {
    text-decoration: underline;
  }

  .eyebrow {
    margin: 0 0 14px;
    color: #c9d8a6;
    font-size: 0.9rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    font-size: clamp(4rem, 10vw, 8rem);
    line-height: 0.9;
    letter-spacing: -0.07em;
  }

  .intro {
    max-width: 720px;
    margin: 28px 0 0;
    color: #ded8c8;
    font-size: clamp(1.15rem, 2vw, 1.45rem);
    line-height: 1.6;
  }

  .link-section {
    margin-top: 72px;
  }

  h2 {
    margin: 0 0 22px;
    font-size: 1.6rem;
  }

  .link-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
  }

  .link-card,
  .link-card:visited {
    display: block;
    border: 1px solid rgba(245, 241, 232, 0.18);
    border-radius: 24px;
    padding: 24px;
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(10px);
    color: inherit;
    text-decoration: none;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      background 180ms ease,
      box-shadow 180ms ease;
  }

  .link-card:hover {
    transform: translateY(-6px);
    border-color: rgba(201, 216, 166, 0.7);
    background: rgba(255, 255, 255, 0.1);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.24);
  }

  .label {
    display: inline-block;
    margin-bottom: 12px;
    color: #c9d8a6;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .link-card strong {
    display: block;
    margin-bottom: 10px;
    font-size: 1.25rem;
  }

  .link-card p {
    margin: 0;
    color: #d5ccb8;
    line-height: 1.55;
  }

 .article-list {
  display: grid;
  gap: 14px;
}

.article-button,
.article-button:visited {
  display: block;
  border: 1px solid rgba(245, 241, 232, 0.18);
  border-radius: 18px;
  padding: 18px 20px;
  background: rgba(255, 255, 255, 0.06);
  color: #f5f1e8;
  font-weight: 800;
  text-decoration: none;
  transition:
    transform 180ms ease,
    border-color 180ms ease,
    background 180ms ease,
    box-shadow 180ms ease;
}

.article-button:hover {
  transform: translateY(-4px);
  border-color: rgba(201, 216, 166, 0.7);
  background: rgba(255, 255, 255, 0.1);
  box-shadow: 0 14px 30px rgba(0, 0, 0, 0.22);
}

.article-button:focus-visible {
  outline: 3px solid #c9d8a6;
  outline-offset: 4px;
}

  .idea-list li {
    border-left: 3px solid #c9d8a6;
    padding: 12px 0 12px 18px;
    color: #ded8c8;
    background: rgba(255, 255, 255, 0.03);
  }

  @media (max-width: 800px) {
    main {
      padding: 56px 0;
    }

    .link-grid {
      grid-template-columns: 1fr;
    }
  }
</style>